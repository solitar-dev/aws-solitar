package org.tobynguyen.solitar.config

/** Central holder for DynamoDB table names (referenced by config + repositories). */
object DynamoTables {
    const val URLS = "urls"
    const val STATISTICS = "statistics"
}
